<h2><%= @task.title %></h2>
<p><%= @task.details%></p>
<%= link_to('Edit Task', edit_path) %>
<%= link_to('Back to Task', tasks_path) %>
<%= link_to "Delete",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
